# Catalog demo fixtures

The seed script defaults to `public/images.jpg`.

Override with any local file:

```bash
npm run db:seed-catalog-demo -- --image path/to/your-watch.jpg
```

Supported extensions: `.png`, `.jpg`, `.jpeg`, `.webp`.
